<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi Tiết Sách</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet"  href="/style.css"> <!--styles sang style -->
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;700&display=swap" rel="stylesheet"> <!--thêm thư viện font-->
</head>
<body>
    <!-- Header -->
    <header>
        <div class="container d-flex justify-content-between align-items-center">
            <div class="header-logo d-flex align-items-center">
                <div> <!--phông chữ mới-->
                    <h1 class="h7 mb-0" style="font-family: 'Dancing Script', cursive; color: #9dea0f;">LyricShelf</h1>
                    <h2 class="h5 mb-0" style="font-family: 'Dancing Script', cursive; color: rgb(189, 231, 22);">Thư Quán Xanh</h2>
                </div>
            </div>
        </div>
    </header>
 <!-- Book Details Section -->
<div class="container my-5">
    <div class="row">
        <!-- Hình ảnh sách -->
        <div class="col-md-4">
            <img src="<%= product.image %>" alt="<%= product.title %>" class="img-fluid rounded shadow">  <!--cải thiện hiệu ứng cho ảnh-->
        </div>

        <!-- Chi tiết sách -->
        <div class="col-md-8">
            <div class="book-details-container">
                <h2><%= product.title %></h2>
                <p><strong>Tác giả:</strong> <%= product.author %></p>
                <p><strong>Năm xuất bản:</strong> <%= product.publishDate %></p>
                <p><strong>Nhà xuất bản:</strong> <%= product.publisher %></p>
                <p><strong>Mô tả:</strong> <%= product.description %></p>
                <p><strong>Giá:</strong> <span class="text-success"><%= product.price %>₫</span></p>
                <button class="btn btn-success">Thêm vào giỏ hàng</button>
            </div>
        </div>
    </div>
</div>

    <!-- Footer --> <!--mới thêm vào footer-->
    <footer>
        <p>&copy; Trường Đại Học Công Nghệ Thông Tin UIT.</p>
    </footer>

    <script src="/scripts.js" defer></script> <!-- thêm phần scripts-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>